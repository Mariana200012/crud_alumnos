services:
  app:
    image: mariana200012/crud_estudiantes:latest
    ports:
      - "5010:5000"
    environment:
      - DATABASE_URL=postgresql://postgres:admin1234@db/cetech
    networks:
      - red_swarm_m

networks:
  red_swarm_m:
    external: true
